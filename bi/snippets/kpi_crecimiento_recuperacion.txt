
//ESTADO

CASE
  WHEN KpiValue("KPI_CRECIMIENTO_RECUPERACION") / KpiGoal("KPI_CRECIMIENTO_RECUPERACION") > 1 THEN 1
  WHEN KpiValue("KPI_CRECIMIENTO_RECUPERACION") / KpiGoal("KPI_CRECIMIENTO_RECUPERACION") <= 1
  AND KpiValue("KPI_CRECIMIENTO_RECUPERACION") / KpiGoal("KPI_CRECIMIENTO_RECUPERACION") >= 0.7 THEN 0
  ELSE -1
END

//TENDENCIA
CASE
  WHEN (KpiValue("KPI_CRECIMIENTO_RECUPERACION"), [DIM FECHA].[ANIO-SEMESTRE-TRIMESTRE-MES-SEMANA].CurrentMember) > (KpiValue("KPI_CRECIMIENTO_RECUPERACION"), [DIM FECHA].[ANIO-SEMESTRE-TRIMESTRE-MES-SEMANA].PrevMember) THEN 1
  WHEN (KpiValue("KPI_CRECIMIENTO_RECUPERACION"), [DIM FECHA].[ANIO-SEMESTRE-TRIMESTRE-MES-SEMANA].CurrentMember) < (KpiValue("KPI_CRECIMIENTO_RECUPERACION"), [DIM FECHA].[ANIO-SEMESTRE-TRIMESTRE-MES-SEMANA].PrevMember) THEN -1
  ELSE 0
END
